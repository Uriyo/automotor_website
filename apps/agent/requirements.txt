# Pinned runtime deps for the AutoMotor agent service.
# Mirrors pyproject.toml — keep both in sync. requirements.txt is here
# for hosts that prefer plain pip workflows (Render, simple Docker, CI).
#
# Install:  pip install -r requirements.txt

fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.1
httpx>=0.27.0

# Agent runtime
langgraph>=0.2.50
langgraph-checkpoint-sqlite>=2.0.0
langgraph-checkpoint-postgres>=2.0.0
langchain-core>=0.3.20
langchain-google-genai>=2.0.0
langchain-openai>=0.2.0

# Backend integrations
supabase>=2.9.0
PyJWT>=2.9.0
psycopg[binary,pool]>=3.2.0
